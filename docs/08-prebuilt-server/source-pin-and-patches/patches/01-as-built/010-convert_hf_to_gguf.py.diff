diff --git a/convert_hf_to_gguf.py b/convert_hf_to_gguf.py
index e09616b..dcfdcc0 100755
--- a/convert_hf_to_gguf.py
+++ b/convert_hf_to_gguf.py
@@ -156,6 +156,10 @@ def parse_args() -> argparse.Namespace:
         "--fp8-as-q8", action="store_true",
         help="Store tensors dequantized from FP8 as Q8_0 instead of BF16/F16.",
     )
+    parser.add_argument(
+        "--fp8-native", action="store_true",
+        help="keep FP8 weights as GGML F8_E4M3 (requires Thor-patched llama.cpp)",
+    )
 
     parser.add_argument(
         "--fuse-qkv", action="store_true",
@@ -294,6 +298,7 @@ def main() -> None:
                                      target_model_dir=Path(args.target_model_dir) if args.target_model_dir else None,
                                      fuse_gate_up_exps=args.fuse_gate_up_exps,
                                      fp8_as_q8=args.fp8_as_q8,
+                                     fp8_native=getattr(args, 'fp8_native', False),
                                      fuse_qkv=args.fuse_qkv,
                                      )
 
