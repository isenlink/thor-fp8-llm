# thor-fp8-llm 仓库维护规则

> **适用对象**：任何负责本仓库后续更新的 AI 助手或人类维护者。
> **当前分工**：T440S 主维护（已提交主体内容）；AI02 待补充自己的测试结果与过程。
> **仓库**：github.com/isenlink/thor-fp8-llm（GitHub 账号 isenlink）
> **当前状态**：private，内容审校期，转 public 需清风最终确认
> **制定日期**：2026-09-11（本版：v1.1，2026-09-11 更新）

---

## 一、内容红线（不可协商）

### 1.1 品牌与车型中立（清风 2026-09-11 明确指示）

**绝对不得出现**以下任何内容：

- 品牌/厂商名：极氪、吉利、Zeekr（任何大小写/语言形式）
- 车型、车版、车辆来历描述（如"量产车拆机件""某车型智驾域控"）
- 渠道商、解锁、刷机、锁机相关内容（整类话题排除在外）

**只讨论**：NVIDIA DRIVE Thor 平台（p3960-0010 / Tegra264 / sm_101a）上的 LLM 部署与优化。

允许保留的"平台固有事实"（不带品牌、只描述现象）：
- 板载 mgbe 车载网口会抢默认路由
- du_*/nv_* 系统预置服务
- AURIX MCU nvshell、CH34x 双串口
- DriveOS 只读根 + overlay 架构

### 1.2 分区路径代称

板载数据分区路径**一律写作 `/brand_data/`**（真实路径名含品牌字样）。

首次出现该代称的文档必须附说明（标准文案）：

> `/brand_data/` 为板载数据分区挂载点在本笔记中的**代称**。DriveOS 板上该分区（约 105G，板载 vblkdev，与只读根分区独立）的原路径名含车辆品牌字样，为保持品牌中立统一写作 `/brand_data/`。读者在自己板卡上执行 `ls /` 即可看到真实分区名。

### 1.3 脱敏规则（每篇文档入仓前逐条执行）

| 类别 | 处理方式 |
|------|----------|
| 板卡 SN 号 | 全删 |
| 内网 IP 地址 | 删除，改拓扑描述或 `<LAN-IP>` |
| MAC 地址 | 删除 |
| 账号名（nvidia/navyyang/senwater/ZeekrNormal 等） | 统一为 `user` |
| 密码/token/凭据/含明文密码的命令（如 sshpass） | 删除并标注 |
| 人名 | 清风→"作者"，AI 助手名→"AI助手" |
| 主机代号 | T440S→"x86主机"，AI01→"转换工作站"，AI02→"交接主机" |
| `/home/senwater/` 等含用户名路径 | 改 `/home/user/` 或 `~/` |
| 内网访问方式/端口/SSH 用法（§7 类内容） | 整节删除 |
| 内部项目代号 | 删除或中性化 |
| 指向仓库外内部文件名的引用（如《xxx.md》） | 改为仓库内相对链接 |

**注意**：`[整理者注]` 复核清单本身也不得罗列原始敏感值（早期曾犯此错——清单里写出 IP/账号等于变相泄露）。

---

## 二、入仓流程

1. **源文档筛选**：工作笔记中挑技术含量高的；CAN 解锁/锁机类一律不进
2. **脱敏改写**：按 §1.3 逐条执行，"坑→现象→根因→解法"结构保留，技术数据（性能数字、参数、命令）100% 保留
3. **格式规范**：
   - 文档开头：英文一句话 Summary + 中文原标题
   - 中文为主，保留表格和代码块
   - 文末附 `[整理者注]` 处理清单（只写"处理了什么类别"，不写原始值）
4. **机器扫描**（不轻信人工/子代理自报，必须 grep 复核）：

```bash
cd ~/thor-llm-notes
# 品牌/车型
grep -rn -iE 'zeekr|极氪|吉利|车版|拆机|智驾域控' --include='*.md' .
# 敏感信息
grep -rn -E '192\.168\.|10\.201\.|senwater|navyyang|nvidia@|ZeekrNormal|清风|小新|文强|sshpass' --include='*.md' . | grep -v 整理者注
```

两条命令输出均应为空（或仅命中"整理者注/代称说明"等合法上下文）。

5. **commit 规范**：说明改了什么、为什么（参考已有 commit 风格）
6. **推送**：`git push origin main`

---

## 三、SSH 推送配置

- 远程：`git@github.com:isenlink/thor-fp8-llm.git`（SSH 协议）
- GitHub 账号 isenlink；**每台机器需各自添加自己的 SSH key**（或 token），不要复用他机私钥
- 网络备注：github.com:22 直连当前稳定；若被墙可切 `ssh.github.com:443` 或代理

---

## 四、目录结构约定（当前实际状态）

```
README.md / README.en.md      中英双语总览
TROUBLESHOOTING.md            踩坑速查（症状索引，30 条，报错原文逐字保留）
docs/
  01-hardware-recon/          板端环境摸底（2 篇）
  02-cross-compile/           交叉编译工具链（3 篇，11 坑详录）
  03-model-conversion/        FP8→GGUF 转换（1 篇）
  04-nvfp4-optimization/      NVFP4 量化与投机解码优化（4 篇）
  05-system-tuning/           系统调优：大页池 + 存储挂载（2 篇）
  06-benchmarks/              基准数据与社区对照（2 篇）
scripts/                      实用脚本：uart_probe + gpu_pool_check（含 README）
```

新增文档放对应目录；新主题开新目录并更新 README 的结构图。

---

## 五、AI02 补充测试结果指引（本次重点）

AI02 需要补充自己的**测试结果与过程**，请按以下约定执行：

### 5.1 放哪里

- **测试结果/基准数据** → `docs/06-benchmarks/ai02-runs/` 目录（新建），每个测试一个 md 文件
- 若是**新的调优方法论/踩坑** → 归入对应主题目录（04/05），并同步更新 `TROUBLESHOOTING.md`

### 5.2 每篇测试文档的必备要素

1. **头部元信息**：英文一句话 Summary + 中文标题 + 日期 + 测试环境（板卡型号、DriveOS/CUDA 版本、模型名、量化档位）
2. **可复现**：完整启动命令、llama-bench 参数、KV 量化设置
3. **结果表格**：pp/prefill、tg/decode、acceptance（如涉及投机解码）、温度、显存/池占用
4. **与已有基线对比**：标注相比仓库现有 26.48 tok/s 基线的提升/变化（README 的 Key results 表）

### 5.3 关键数据口径（与现有文档保持一致）

- 测速用 `llama-bench`，**必须 `--parallel 1`**（否则基线虚低 20%）
- decode 速度分"高可预测任务"和"创作任务"两类分别报（投机解码下差异巨大）
- 大页池单位统一 GiB；池大小对比参照 README 的"20G→42G→46G"
- 温度：空载 61-66°C 正常，>75°C 暂停

### 5.4 必须遵守

- §1.3 脱敏规则**同样适用**（AI02 的测试可能涉及板卡、IP、账号）
- `/brand_data/` 代称 + 说明文案
- 报错原文逐字保留（如遇新坑，进 TROUBLESHOOTING.md 时保留英文原文）
- 不含任何品牌/车型/解锁信息

---

## 六、转 public 前的最终检查（单独一轮，不与日常更新合并）

1. 用 §二.4 的扫描命令全仓跑一遍
2. **人工通读**一遍每篇文档（机器扫描有盲区：语义级泄露，如"我司""我们车"、仓库外文件引用、内部协作黑话）
3. 敏感信息清单交清风过目
4. **清风明确说"可以公开"后才执行** `gh repo edit --visibility public`（或网页操作）
5. public 后 git 历史不可撤销——转 public 前确认历史 commit 中也无敏感信息（本仓库 private 期间所有 commit 均已按本规则执行，理论安全，但仍需抽查早期 commit）

---

## 七、待办清单（当前）

- [x] scripts 挑拣（uart_probe + gpu_pool_check 已入仓）
- [x] 大页池扩容专篇（hugepage-pool.md）
- [x] README 英文完整版（README.en.md）
- [x] TROUBLESHOOTING.md 踩坑速查（30 条）
- [ ] **AI02 补充测试结果与过程**（§五）← 当前接力棒
- [ ] 转 public 最终检查（§六）
